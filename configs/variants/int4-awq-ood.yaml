name: int4-awq-ood
family: ptq
track: B
backend:
  inference_backend: vllm
  weight_dtype: int4
  kv_cache_dtype: fp16
  gpu_offload_layers: -1
quant:
  method: llmcompressor-awq
  bit_width: 4
  group_size: 128
  calibration_corpus: ood
  calibration_samples: 512
  calibration_seed: 620       # the draw that selects the samples; hashed with the corpus
