name: fp16
family: baseline
track: A
backend:
  inference_backend: hf
  weight_dtype: fp16
  kv_cache_dtype: fp16
  gpu_offload_layers: -1   # not applicable outside llama.cpp
# The reference point. Every other variant is measured against this row.
