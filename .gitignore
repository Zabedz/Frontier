# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environments
.venv/
venv/
.env
.python-version.local

# Editors
.idea/
.vscode/

# uv
# uv.lock is committed on purpose (reproducibility); nothing to ignore here.

# Local machine change log for remote pods (kept local, never shared)
SSH_CHANGELOG.md

# Large / generated experiment artifacts
# Result rows are committed (they are the record). Heavy binaries are not.
models/
checkpoints/
*.gguf
*.safetensors
*.bin
teacher_cache/            # cached teacher top-k logits for offline distillation
runs/                     # raw per-run scratch (logs, temp checkpoints)
results/**/*.parquet.tmp
plots/*.png.tmp

# OS
.DS_Store
