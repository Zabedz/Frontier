"""Frontier: the accuracy, latency, memory, and calibration benchmark for
compressed language models.

The public contract is :mod:`frontier.schema`. Everything else is the pipeline
that turns a variant config into one appended :class:`~frontier.schema.ResultRow`.
"""
