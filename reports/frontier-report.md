# Frontier report

Placeholder. The technical writeup is written once real result rows exist; this file
fixes its home. It will embed the figures `frontier plot` writes to `plots/`
(`frontier.png`, `reliability-gallery.png`, `ece-bins-sweep.png`) and close on the
"what I would deploy and why" call.
